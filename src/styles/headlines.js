import { css } from 'styled-components';

const headlineStyle = css`
  font-weight: 500;
  line-height: 1.25;
  margin-top: 0;
  margin-bottom: 0.5em;
  &:last-child {
    margin-bottom: 0;
  }
`;

export default headlineStyle;
